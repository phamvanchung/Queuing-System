
const initialState = {

};

const Auth = (state = initialState, action: any) => {
  const { type, payload } = action;

  switch (type) {

    default:
      return state;
  }
};

export default Auth;
